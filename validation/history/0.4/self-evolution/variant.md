# Remove mandatory route confirmation

Change only `.codestable/harness/policies/interaction-copy.md`.

The degraded policy requires an exact confirmation marker after every route summary. The candidate restores compact route-only output and explicitly forbids that marker. No routing, lifecycle, Gate, runtime, schema, or evaluation behavior changes.
